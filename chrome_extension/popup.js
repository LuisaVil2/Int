const s=document.getElementById('status');function refresh(){chrome.runtime.sendMessage({type:'status'},r=>{s.textContent=`Connection: ${r?.connected?'online':'offline'} | Mic: ${r?.mic?'on':'off'} | Latency: ${Math.round(r?.latency||0)}ms${r?.lastError?' | '+r.lastError:''}`;});}document.getElementById('start').onclick=()=>chrome.runtime.sendMessage({type:'start'},refresh);document.getElementById('stop').onclick=()=>chrome.runtime.sendMessage({type:'stop'},refresh);setInterval(refresh,1000);refresh();
